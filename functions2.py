def absoloute_value (num):
    if num >= 0:
        return num
    else:
        return -num

print("98's absoloute value is ",absoloute_value(98))
print("-98's absoloute value is ",absoloute_value(-98))